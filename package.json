{
  "name": "jte-recruit-website",
  "private": true,
  "description": "Static marketing site for jte.com.sg — hand-authored HTML + generated salary/blog pages + prebuilt Tailwind CSS. Run `npm run build` after any HTML/class change.",
  "scripts": {
    "blog": "node scripts/build-blog.mjs",
    "salary": "node build-salary.mjs",
    "css": "npx --yes tailwindcss@3.4.17 -c tailwind.config.cjs -i assets/tw-input.css -o assets/tailwind.css --minify",
    "build": "npm run blog && npm run salary && npm run css"
  }
}
